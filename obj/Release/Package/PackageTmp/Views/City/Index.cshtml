@model IEnumerable<BHShows.Models.City_T>

@{
    ViewBag.Title = "Index";
}
<script src="@Url.Content("~/js/jquery-1.8.3.min.js")"></script>
<div class="container-fluid">
    <div class="page-content">
        <div class="row" style="min-height:850px;">
            <div class="col-lg-12">
                <section class="panel">

                    <p>
                        <a href="@Url.Content("~/City/Create")" class="btn btn-danger pull-right">Create</a>
                    </p>
                    <br class="clearfix" />
                    <br class="clearfix" />
                    <table class="table">
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.City)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Country_T.Country)
                            </th>
                            <th></th>
                        </tr>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.City)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Country_T.Country)
                                </td>
                                <td>
                                    @Html.ActionLink("Edit", "Edit", new { id = item.CityID }) |
                                    @Html.ActionLink("Details", "Details", new { id = item.CityID }) |
                                    @Html.ActionLink("Delete", "Delete", new { id = item.CityID })
                                </td>
                            </tr>
                        }

                    </table>
                </section>
            </div>



        </div>
    </div>
</div>
